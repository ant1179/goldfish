# Server URL for the backend service
# In development, this should point to the server container
# In production, use the actual server URL
SERVER_URL=http://localhost:8000

# Client port configuration
CLIENT_EXTERNAL_PORT=3000
CLIENT_INTERNAL_PORT=8080